//  mybrute force approaach 


class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int n= nums.size();
      
        for (int i =0;i<n;i++){
              int numm=0;
            for(int j=0;j<n;j++){
                 if (nums[j] == nums[i]) {
                numm++;
            }
        }
        if(numm>(n/2)){
            return nums[i];
        }
      }
         return -1;
        
    }
};
